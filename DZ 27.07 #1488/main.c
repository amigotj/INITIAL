#include <stdio.h>
#include <math.h>

int main(int argc, char *argv[])
{
	int n,m;		// n - hours m - minutes  kogda strelki sovpadut
	scanf("%d",&n);
	scanf("%d",&m);

	if ((n<=12) && (n>0) && (m<60) && (m>=0)) 
	printf("%d\n",);
	return 0;
}


